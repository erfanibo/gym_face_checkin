# سیستم لوکال تشخیص چهره — پذیرش باشگاه

## مسئله
پذیرش باشگاه به یک راه سریع‌تر از چک دستی کارت/اسم برای تشخیص مشتریان قدیمی و ثبت‌نام مشتریان جدید نیاز داشت — بدون وابستگی به سرویس ابری (به‌خاطر حریم خصوصی داده‌های بیومتریک و هزینه).

## راه‌حل
یک سیستم کاملاً لوکال (بدون هیچ تماس با سرویس بیرونی) که:
- با یک وب‌کم معمولی در ورودی، چهره‌ی هر فرد را لحظه‌ای پردازش می‌کند.
- اعضای قدیمی را خودکار شناسایی و تردودشان را ثبت می‌کند.
- چهره‌های جدید را در یک «صف انتظار» زنده به اپراتور پذیرش نشان می‌دهد تا با چند کلیک ثبت‌نامشان کند.

## جریان کاری
1. دوربین ورودی به‌صورت مداوم فریم می‌گیرد.
2. موتور تشخیص چهره (Python + OpenCV + `face_recognition`) هر چند فریم یک‌بار، صورت‌ها را پیدا کرده و به بردار عددی (Embedding) تبدیل می‌کند.
3. بردار با پایگاه‌داده اعضا مقایسه می‌شود:
   - **تطبیق پیدا شد →** تردد ثبت می‌شود (با محدودیت زمانی برای جلوگیری از ثبت تکراری).
   - **تطبیق پیدا نشد →** عکس + بردار در «صف انتظار» ذخیره می‌شود و فوراً روی پنل پذیرش نمایش داده می‌شود.
4. اگر همان فرد ناشناس چند دقیقه جلوی دوربین بماند، به‌خاطر مکانیزم آنتی‌اسپم دوباره در صف تکرار نمی‌شود.
5. اپراتور پذیرش با کلیک روی عکس، فرم کوتاهی (نام، تلفن، کد عضویت) پر می‌کند و فرد از صف انتظار به «عضو رسمی» تبدیل می‌شود.

## پشته فناوری
| بخش | تکنولوژی | چرا |
|---|---|---|
| هستهٔ تشخیص چهره | Python, OpenCV, `face_recognition` (dlib) | استاندارد رایج و متن‌باز برای این کار، کاملاً لوکال |
| بک‌اند | FastAPI + WebSocket | سریع، ساده، پشتیبانی بومی از بروزرسانی زنده |
| پایگاه‌داده | SQLite | برای زیر ۲۰۰ عضو کاملاً کافی، بدون نیاز به سرور جدا |
| پنل پذیرش | HTML/JS ساده | بدون نیاز به build، به‌سرعت روی هر مرورگری کار می‌کند |
| استقرار | Docker (روی PC لینوکسی کنار دوربین) | یک‌بار تنظیم، همیشه یکسان اجرا می‌شود |

## وضعیت فعلی
✅ هستهٔ تشخیص چهره، پایگاه‌داده، API، پنل پذیرش، و Docker — همه پیاده‌سازی و تست منطقی شده‌اند.
✅ ریپوی Git محلی آماده و منتظر push به GitHub.
🔲 هنوز تست میدانی با دوربین و نور واقعی محل انجام نشده (گام بعدی).
🔲 پنل پذیرش فعلاً بدون لاگین اپراتور است (تصمیم آگاهانه برای فاز اول).

## محدودیت‌های شناخته‌شده
- یک دوربین به ازای هر نمونه از سیستم (نه چند شعبه هم‌زمان).
- کیفیت تشخیص در نور کم یا با ماسک/عینک افت می‌کند — محدودیت خود کتابخانه، نه باگ.
- SQLite برای این مقیاس (زیر ۲۰۰ نفر) مناسب است؛ رشد بیشتر نیاز به Postgres دارد.

## گام‌های بعدی پیشنهادی
1. تست با دوربین و نور واقعی باشگاه و کالیبراسیون آستانه‌های تطبیق.
2. افزودن لاگین ساده برای پنل پذیرش قبل از استفادهٔ عملیاتی.
3. push کردن به GitHub و راه‌اندازی روی PC نهایی با Docker.
